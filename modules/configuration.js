import { placeholderHeader1, placeholderText } from './strings.js';

export const inputInitialConfiguration = {
	height: '25px',
	fontSize: '14px',
	placeholder: placeholderText,
};

export const headersConfiguration = {
	1: { height: '30px', fontSize: '30px', placeholder: placeholderHeader1 },
};

export const attributesList = { placeholder: placeholderText, class: '' };
